/**
 * Basic settings for running the app are in here.
 * Do not put anything sensitive (API Keys, Passwords, etc)
 * in this file.
 */

const SERVER_PORT = '3000';
const GAPI_PATH = `http://devgapi.enetdefender.com/graphql`;

module.exports = {
  SERVER_PORT,
  GAPI_PATH,
};
